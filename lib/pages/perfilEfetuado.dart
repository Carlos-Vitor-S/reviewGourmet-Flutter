import 'package:flutter/material.dart';

class PerfilEfetuado extends StatelessWidget {
  const PerfilEfetuado({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Login Efetuado"),
    );
  }
}
